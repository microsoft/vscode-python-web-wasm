variables:
  name: buildSteps
  value:
    - script: |
        npm ci
      displayName: "Install dependencies"

    - script: |
        npm run lint
        npm run compile
      displayName: "Lint & Compile"
